spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bankcore_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

app:
  jwt:
    secret: "bankcore-secret-key-para-hs256-debe-tener-minimo-256-bits-de-longitud"
    expiration: 86400000